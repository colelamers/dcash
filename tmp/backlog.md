add lower level bar showing balances
display account tree
add date locale
enable split transactions?
add color to rows; every other, custom?
add custom colors to accounts
ensure math summation and rounding is correct at bottom
collectivise under a tree (nodes?)
create new row items using ncurses, maybe add file top thing? add new? hotkey? i like the file menu dropdown with hotkey
add my rollover budget feature
add projections
add render to pdf?
add charts and plotting?
wrap text on edit_account memo
wrap text on account memo section
add line chart?
fix edit_account to handle larger sections of text
fix edit_window to wrap text
allow for memo field in account to wrap text
add bar chart?
enable scrolling
set date field as setting modifiers
color text shown
add the ability to navigate editable text by hitting arrow keys. so right/left lets you jump to words etc
properly setup OOP public and private actions
clean up unused includes
long way down the road, but support for CYGWIN or whatever is required for windows
add options/settings flags for tcash.xml flags. date default display setting (yyyy-dd-MM, dd-MM-yy, MM/dd/yy, etc.)
order by columns
auto save feature
cleanup headers in files
add sibling items
# update account balance upon exit
# show an account (with balance)
# fix ifndefs
